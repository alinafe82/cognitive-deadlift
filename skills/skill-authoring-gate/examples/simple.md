# Simple Example

Input:

```text
Review a proposed skill named api-docs-check before adding it.
```

Expected behavior:

- Identify the repeated problem the skill solves.
- Check trigger and non-trigger boundaries.
- Require examples and failure-mode behavior before approval.
