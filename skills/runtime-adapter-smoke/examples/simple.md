# Simple Example

Input:

```text
A new skill was added and .claude-plugin/plugin.json was updated.
```

Expected behavior:

- Check the Claude list includes the new skill path.
- Confirm Codex still points at ./skills/.
- Run the repo validator and report pass or failure.
