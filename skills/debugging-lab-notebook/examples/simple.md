# Simple Example

Input:

```text
The CLI sometimes exits zero after failure.
```

Expected behavior:

- Build a repeatable command or observation signal.
- List hypotheses around exception handling and exit-code mapping.
- Run one experiment at a time.
- Add regression proof after the fix.
