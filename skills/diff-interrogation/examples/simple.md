# Simple Example

Input:

```text
Review this diff that changes validation from < to <=.
```

Expected behavior:

- Describe the accepted-input behavior change.
- Identify boundary tests that should exist.
- Ask whether downstream callers depend on the old boundary.
