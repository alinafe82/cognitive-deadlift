# Simple Example

Input:

```text
Should this timeout become a config flag?
```

Expected behavior:

- Option A: keep a local constant and add a test.
- Option B: add a config flag with validation.
- Option C: defer the change until there is a second timeout consumer.
- Recommendation names reversibility and test impact.
