# Edge Case Example

Input:

```text
Prepare a public plugin release after changing licenses and runtime manifests.
```

Expected behavior:

- Check license and notice consistency.
- Smoke affected adapters.
- Block release if public docs overstate runtime compatibility.
