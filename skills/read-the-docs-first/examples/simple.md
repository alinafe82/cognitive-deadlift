# Simple Example

Input:

```text
Update this GitHub Action because it supports Python 3.13 now.
```

Expected behavior:

- Check local workflow and action version.
- Check the action's public docs or release notes if browsing is available.
- Separate documented support from inference.
