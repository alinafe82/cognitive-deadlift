# Edge Case Example

Input:

```text
Gemini uses skills the same way Claude does.
```

Expected behavior:

- Check local Gemini adapter files.
- Distinguish shared `SKILL.md` content from Gemini context loading.
- Record the adapter boundary rather than claiming identical runtime behavior.
