# Edge Case Example

Input:

```text
Compare a proposed plan-check skill against problem-framing, assumption-audit, and alternatives-before-code.
```

Expected behavior:

- Identify overlapping plan validation behavior.
- Recommend merging into assumption-audit unless the new skill has a different evidence contract.
- Name exact boundaries if keeping it.
