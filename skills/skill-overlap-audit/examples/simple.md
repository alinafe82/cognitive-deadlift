# Simple Example

Input:

```text
Compare a proposed docs-claim-audit skill against read-the-docs-first.
```

Expected behavior:

- Show that read-the-docs-first gathers sources before claims.
- Show that docs-claim-audit verifies public claims before merge.
- Recommend distinct trigger wording if both jobs remain useful.
