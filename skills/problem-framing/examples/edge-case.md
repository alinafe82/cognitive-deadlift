# Edge Case Example

Input:

```text
Move billing sync to a queue because customers are missing invoices.
```

Expected behavior:

- Separate the missing-invoice problem from the queue proposal.
- Ask what evidence shows billing sync is the cause.
- Define success as invoices generated correctly for the affected workflow.
- Mark the queue as an option, not the problem statement.
