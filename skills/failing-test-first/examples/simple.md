# Simple Example

Input:

```text
The parser accepts empty input.
```

Expected behavior:

- Define a failing test for empty input.
- Run the test before editing parser logic.
- Implement the smallest fix.
- Re-run the failing test and nearby parser tests.
