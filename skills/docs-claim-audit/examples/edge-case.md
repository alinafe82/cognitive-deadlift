# Edge Case Example

Input:

```text
Docs claim runtime compatibility with Codex, Claude, and Gemini after only manifest validation.
```

Expected behavior:

- Separate manifest consistency from actual runtime smoke testing.
- Require limitation language or smoke evidence.
- Avoid overstating compatibility.
