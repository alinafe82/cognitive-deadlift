# Simple Example

Input:

```text
README says make prod-gate runs all meaningful checks.
```

Expected behavior:

- Verify Makefile prod-gate target.
- Confirm listed checks match the command.
- Keep the claim if evidence matches.
